#include <stdio.h>
#include "head.h"

int main(int argc, const char *argv[])
{
    int x = 20, y = 10;
    int max = 0;

    max = find_max(x, y);
    printf("M %d\n", M);
    printf("max %d\n", max);

    return 0;
}