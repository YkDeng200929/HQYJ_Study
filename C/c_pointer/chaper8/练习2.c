#include <stdio.h>

#define TRUE 1
#defien FALSE 0

int find_char( char **strings, char value)
{
    char *string;//当前正在查找的字符串
    // 对于列表中的每个字符串
    while ((string = *string++) != NULL)
}

int main()
{
}