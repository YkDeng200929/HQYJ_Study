作业1:1、写一个sum()函数，要不传递参数，函数中要求打印出，1+2+3+...+100的值。
     2、写一个flower()函数，要求不传递参数，函数中要求打印出 100~999中所有的水仙花数。
             所谓水仙花数：一个三位数，其个位，十位，百数上的数字的立方和等于该数本身
     3、main()函数中调用sum(),flower()函数。查看我们得到的结果。
     
作业2：
1、定义一个int square（int a)函数,要求该函数实现求正方形面积的功能，a * a,
      并把得到的结果通过return返回给主函数。
2、主函数从键盘输入一个数据，把数据传递给函数，
	 然后调用函数接收函数返回值，打印返回值结果。     
     
     
作业3：
定义全局变量 global_data = 0;
设计无参数的函数global_add()，在这个函数内部
实现对全局变量global_data++,累计操作100次
在主函数调用这个函数，并输出global_data的值    


作业4：
<1>（1）写一个函数int find_max(int a,int b)，要求该函数实现功能，
			  找到两个a,b两个值中的最大值，找到之后return返回给主函数
   （2）主函数要求从键盘输入两个数据，并将输入的两个数据传递给我们的find_max函数中。
        定义max变量来接收我们函数的返回值，并且打印。   
        
        
<2>1.写一个函数test（int m）, 要求函数实现功能：判断m是奇数还是偶数，若是奇数，
		求1 + 3 + 5 + ...+m的的值，若是偶数，求0 + 2 + 3 + ...+m的值.
		并把得到的结果传递给主函数
	2.主函数，从键盘从键盘输入一个数据，给test（）函数传参，并接收函数返回值打印。


作业5：
int data1 = 10，data2 = 20大家自己来写一个函数，
在my_swap()函数中实现 data2 = 10,data1 = 20；
并且函数结束后，在主函数中打印依旧如此。
看看大家能写几种交换的方法。


作业6：
int main（）函数中定义如下变量
 int arry[7] = {10,23,45,78,90,4,6};
int max,min;

在定义一个函数
find_max_min()实现一个功能：
找到数组中最大的值存放在max中，找到最小的值存放在min中

作业7:
(1)主函数定义一个数组，int a[5] = {5,4,3,2,1}；
要求写一个函数sort（）,实现对数组元素进行从小到大排序
排序，排序之后，主函数输出数组的内容为1,2,3,4,5

作业8：
1、定义一个array_function()函数,函数中定义一个数组int a[5] = {5,4,3,2,1};
   int *array_function()
   {
   }
2、在定义一个sort()函数，要求用冒泡排序对数组a中的数据进行从小到大排序，要求
排序后数组的内容为{1,2,3,4,5}

3、主函数中，调用array_function函数，打印数组中的数据，
                    在调用array_function函数，再次打印数组中的数据。

作业9：
定义几个函数
1、长方形函数
      int rectangle();
2、正方形函数
     int quadrate();
3、三角形函数
     int triangle();
4、求面积的函数
    int area();
5、主函数
    int  main（）

要求：1、从键盘上输入长方形的长、宽，然后输出面积
   2、从键盘上输入正方形的长、宽，然后输出面积（注意，正方形的长和宽是一样的）
   3、从键盘上输入三角形的低和高，然后输出面积
   
注：以上代码要求用函数指针实现求我们长方形、正方形、三角形的面积。

作业：大家来用递归的方式来写一个5！来看看。


作业：1.定义一个结构体
struct student
{
        int     id;
        char  name[10];
        short  score；
};

2.产生一个学生类型结构体数组struct student stu[3];
3.设计一个函数input_student , 从键盘输入3个学生的信息 
4.设计一个函数output_student , 输出学生的信息


作业：
         1、大家来设计一个查询学生信息共用体。包含id，name，sex。
              union stu
             {
                    int  id;
                    char name[5];
                    char sex;
            };
            printf("please select  [id,name,sex] ： ");
             从键盘输入数据，若是输入id，输出id = 15；
                                         若是输入name，输出name = "lilei"
                                         若是输入sex，那么输出sex='m'
                                         
                                         
练习：
1、设计一个联合体，有三个成员，给最大的成员赋值为0x12345678;以十六进制的形式输出其他成员的值看看
union stu
{
  int a;
  short b;
  char c;
}
2、设计一个枚举类型，用户从键盘输入数字，打印数字对应的颜色。
enun COLOR
{
        red,
        green,
        yellow,
        blue,
        black
};                                         
                                         
                                         
               