#include <stdio.h>

void quicksort(int arr[], int first, int last)
{
    int low = first;
    int high = last;
    int key = arr[first];
    if (first >= last)
    return ;
    while ()
}

int main(void)
{
    return 0;
}